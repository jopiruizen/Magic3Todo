const styles = (theme) => ({

    fullView: {
      display: 'block',
      backgroundColor: '#FAFAFA',
      width: '100%',
      height: '100%',
    },

    todoPage: {
      height: '100%',
    },

    mainTodo: {
      backgroundColor: '#EFEFEF',
      height: '100%',
    },

    subTodo: {
      backgroundColor: '#FF000',
    },
});

export default styles;